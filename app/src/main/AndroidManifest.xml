<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apmob">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apmob.NoActionBar">
        <activity android:name=".Set_reminder"></activity>
        <activity android:name=".mainMenu_activity" />
        <activity android:name=".Regis_activity" />
        <activity android:name=".Sign_in" />
        <activity android:name=".Login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Regis_activity">

        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".Agenda" />
    </application>

</manifest>